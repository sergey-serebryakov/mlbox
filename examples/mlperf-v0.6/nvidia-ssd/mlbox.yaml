type: mlbox
mlbox_spec_version: 0.1
name: MLPerf Training v0.6 - NVIDIA SSD Benchmark
version: 0.1
author: MLPerf Best Practices Working Group
implementation: exec
tasks:
  benchmark:
    inputs:
      parameters_file:
        description: "File with environment variables."
      data_dir:
        description: "Directory for COCO 2017 dataset."
    outputs:
      log_dir:
        description: "Directory for MLPerf log files."
